# DAG Harness Environment Configuration
# Copy this to .env and fill in your values

# GitLab Configuration (optional if using glab auth)
# GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx
# GITLAB_PROJECT=tinyland/projects/dag-harness
# GITLAB_GROUP=tinyland

# Database path (defaults to harness/harness.db)
# HARNESS_DB_PATH=/path/to/harness.db

# Notifications (optional)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
# EMAIL_RECIPIENT=you@example.com

# KeePass Configuration (for credential lookups)
# KEEPASS_DATABASE_PATH=/path/to/database.kdbx
# KEEPASSXC_DB_PASSWORD=your_password
